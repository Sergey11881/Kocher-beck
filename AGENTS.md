Правила постоянной работы над проектом:

- Основное Expo-приложение: `artifacts/print-fixture-orders`.
- `artifacts/kocher-beck-ios-test` считать устаревшим проектом, пока отдельно не будет принято решение о его удалении или исключении.
- НИКОГДА не изменять, удалять, пересоздавать или коммитить `artifacts/api-server/data/toolorder.db` без отдельного явного разрешения пользователя.
- Не удалять существующую функциональность без явного требования.
- Перед изменением сначала изучать существующую архитектуру и связанные файлы.
- После каждого изменения запускать релевантные проверки.
- Для основного Expo-приложения обязательно запускать typecheck.
- Проверять `git diff` после изменения.
- Не коммитить незавершённую работу.
- Каждую завершённую логическую задачу оформлять отдельным commit.
- Не использовать OpenAI API и не добавлять API-ключи.
- Не менять секреты и credentials.
- При неоднозначной задаче сначала объяснять найденную проблему и безопасный вариант решения.
- Стараться делать небольшие обратимые изменения.
- Перед архитектурными изменениями учитывать существующий API, OpenAPI и generated client.
- Не считать задачу завершённой, пока проверки не прошли или причина отказа не объяснена.